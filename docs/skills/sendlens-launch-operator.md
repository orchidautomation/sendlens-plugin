# `sendlens-launch-operator`

Gates launch, resume, clone, scale, pause, or stop decisions and defines the campaign's measurement, guardrails, decision rules, and learning or client handoff.

It remains read-only: it recommends provider configuration and actions but never mutates campaigns. Broad analyst requests invoke it automatically after strategy and copy when an operating plan is requested.
