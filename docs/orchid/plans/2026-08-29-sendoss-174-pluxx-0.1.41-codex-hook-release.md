# SENDOSS-174 — rebuild and release Codex SessionStart with Pluxx 0.1.41

## Objective

After `@orchid-labs/pluxx@0.1.41` is immutably published and verified, update SendLens from its current Pluxx pin to 0.1.41, regenerate all host bundles, release the next SendLens patch (currently expected 0.1.88), and prove the installed Codex `SessionStart` command launches safely without `CODEX_PLUGIN_ROOT`.

## Authority and routing

- Linear issue: SENDOSS-174
- Primary repository: `orchidautomation/sendlens-plugin`
- Base branch: `main`
- Planning branch: `codex/sendoss-174-ready-pluxx-0.1.41`
- Supporting dependency: `orchidautomation/pluxx` read-only; Pluxx 0.1.41 released from trusted merge `0379d2c646ad9b83fb31016d9d553ba5dea96e76`
- Risk: Elevated — generated hook packaging, release artifacts, and credential-sensitive startup behavior
- Orchid route after dependency proof: `implement-linear-task` through hosted Orchid compatibility label `delegate:codex`

## Hard precondition

Do not start implementation until all are true:

1. npm reports exact `@orchid-labs/pluxx@0.1.41` and registry integrity;
2. GitHub has immutable Pluxx tag/release `v0.1.41` bound to trusted `main` containing PLUXX-345;
3. the Pluxx release receipt identifies exact artifact and target verification;
4. SENDOSS-174's PLUXX-345 blocker is removed only after those fresh readbacks.

Merged source alone is not a consumable release.

### Precondition readback — satisfied 2026-08-29

- npm: `@orchid-labs/pluxx@0.1.41`, integrity `sha512-m08Sr20N2SzohxySOSETpuQQlVVEFqyubreONy2KTWvzz4JHr4nPueXgOmYJeKC1Tmuij3Odqwk767hvhK+YcA==`
- immutable tag/release: `v0.1.41` at `0379d2c646ad9b83fb31016d9d553ba5dea96e76`
- workflow: GitHub Release run `33254601754` passed
- byte identity: npm and GitHub tarballs share SHA-256 `25e0039ad63f10a79970d8028c56461f9813cbfb522088be93beba18bde6508e`
- isolated registry CLI: `npx --yes @orchid-labs/pluxx@0.1.41 --version` returned `0.1.41`
- release receipt: `orchid.release.receipt/v1`, digest `9f12122842a778fe86ae06093cc9b3e5b92416c9a41f055a5e3f21bb15f7b8cb`

## Implementation

1. **Adopt the verified compiler release**
   - Update `package.json` and `package-lock.json` from the current Pluxx pin to exact 0.1.41.
   - Confirm the lockfile tarball and integrity match the authenticated registry result.

2. **Regenerate the four host bundles**
   - Run the canonical Pluxx build from `pluxx.config.ts`; do not hand-edit generated wrapper files.
   - Inspect Codex `hooks/hooks.json` and confirm `SessionStart` launches through `${PLUGIN_ROOT}` rather than `${CODEX_PLUGIN_ROOT}`.
   - Preserve Claude Code, Cursor, and OpenCode output behavior.

3. **Add exact manifest-command proof**
   - Extend the maintained host-bundle test so it reads and executes the exact generated Codex `SessionStart` command from an unrelated workspace with `CODEX_PLUGIN_ROOT` absent.
   - Use a credential-free/demo fixture. Assert exit 0 and preserve the existing safe-skip message when provider credentials are absent.
   - Preserve configured-provider refresh locking and demo behavior without invoking a live provider or exposing credentials.

4. **Prepare the SendLens release**
   - Advance both package manifests from 0.1.87 to the next still-unreleased patch (expected 0.1.88; re-read current `main` immediately before editing and advance again if another PR has landed).
   - Update release/QA evidence to reference PLUXX-345, completed SENDOSS-56, and SENDOSS-174.
   - Run the full host-bundle and release gates on the exact PR head.

5. **Post-merge target proof**
   - The version-gated `main` workflow owns publication after Brandon merges.
   - Verify tag, release assets, checksum/integrity, and installed Codex cache.
   - Start a fresh Codex Desktop thread and prove no SendLens `SessionStart` failure while keeping keys and customer data out of evidence.

## Owned paths

- `package.json`
- `package-lock.json`
- `pluxx.config.ts`
- maintained generated host bundles
- `scripts/test-host-bundle-inventory.mjs`
- focused config-loader/bundle tests needed for the exact manifest command
- release/install compatibility tests directly affected by regenerated bundles
- `docs/RELEASING.md` and issue-specific `docs/orchid/qa/` evidence

## Forbidden paths and actions

- No Pluxx repository changes.
- No provider API, campaign analytics, database schema, MCP response, refresh semantic, or commercial-repository change.
- No live provider refresh, credentials, customer payloads, private replies, or raw campaign data in tests/logs.
- No manual merge, tag, publish, install, Linear Done/Shipped, or active Codex cache mutation from the implementation lane.

## Acceptance criteria

- Both package manifests pin verified Pluxx 0.1.41 with matching lockfile integrity.
- Generated Codex `SessionStart` uses `${PLUGIN_ROOT}` and the exact descriptor exits 0 from an unrelated workspace with `CODEX_PLUGIN_ROOT` absent.
- Credential-free startup preserves the safe-skip message and exit 0.
- Demo/configured refresh and lock behavior remain green without live provider mutation.
- Claude Code, Cursor, and OpenCode bundle tests remain green.
- Both SendLens version surfaces advance together to an unreleased patch.
- `npm run test:host-bundles`, legacy installer compatibility, and `npm run release:check` pass at the exact PR head.
- The PR receives `ai:autofix-enabled` as required by repo policy and Brandon's delegation request.
- Release/install/Desktop proof is complete before SENDOSS-174 becomes Shipped/Done.

## Validation

- `npm ci`
- `npm run build:hosts`
- `npm run test:host-bundles`
- `npm run test:legacy-installer-compat`
- `npm run release:check`
- inspect built and installed Codex `hooks/hooks.json`
- execute the installed manifest command with `CODEX_PLUGIN_ROOT` unset in a credential-free fixture
- fresh Codex Desktop thread with no SendLens startup-hook failure

## Delivery

One issue-bound branch/worktree and one PR to `main`. Stop for Brandon's merge. Publication, installation, target verification, and lifecycle Shipped/Done remain separate post-merge Orchid Release boundaries.
