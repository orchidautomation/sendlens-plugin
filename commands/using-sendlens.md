---
description: "SendLens: load the MCP-first routing and evidence contract"
argument-hint: "[sendlens-question]"
agent: using-sendlens
subtask: false
---

# Using SendLens

Use the `using-sendlens` skill when the user asks how SendLens should route an analysis question, what evidence limits apply, or which workflow should handle a SendLens request.

Arguments: $ARGUMENTS

If arguments are provided, classify the request into the right SendLens workflow and name the first MCP tool surface to use.
