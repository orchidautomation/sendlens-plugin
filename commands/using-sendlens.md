---
description: "SendLens: load the MCP-first analyst routing, evidence, and decision contract"
argument-hint: "[sendlens-question]"
agent: campaign-analyst
subtask: false
---

# Using SendLens

Use the `sendlens-analyst` skill when the user asks how SendLens routes an analysis question, what evidence limits apply, or which workflow module should handle a SendLens request.

Arguments: $ARGUMENTS

If arguments are provided, classify the request into the right SendLens workflow and name the first MCP tool surface to use.
