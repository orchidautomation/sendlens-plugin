---
description: "SendLens: gate campaign launch or scale and define measurement, stop, and learning rules"
argument-hint: "[campaign-or-approved-package]"
subtask: false
---

# SendLens Launch Operator

Use the `sendlens-launch-operator` skill to assess launch, resume, clone, scale, pause, or stop readiness and define the operating contract.

Arguments: $ARGUMENTS

Return blockers first, then configuration, monitoring, guardrails, stop/iterate/scale rules, and the learning handoff.
